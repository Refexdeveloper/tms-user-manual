# Shared / API
NODE_ENV=development
PORT=8080
APP_BASE_URL=http://localhost:8080
JWT_SECRET=change-me-in-production
CORS_ORIGIN=http://localhost:5173

# Database — local SQLite file path OR Cloud SQL Postgres URL
# Local:
DATABASE_URL=file:./data/tms.sqlite
# GCP Cloud SQL example:
# DATABASE_URL=postgresql://tms_user:PASSWORD@/refex_tms?host=/cloudsql/PROJECT:REGION:INSTANCE

# Attachments — local disk OR GCS bucket
STORAGE_DRIVER=local
LOCAL_UPLOAD_DIR=./uploads
# GCP:
# STORAGE_DRIVER=gcs
# GCS_BUCKET=refex-tms-attachments-prod
# GCS_PROJECT_ID=your-gcp-project

# Flight search (Travolution via existing Cloud Run)
FLIGHT_API_BASE=https://refex-tms-flightsearch-dhwffeu7pq-el.a.run.app

# Kissflow BFF (Option B) — NEVER put these in the browser / Vite env
KISSFLOW_DOMAIN=https://refexgroup.kissflow.com
KISSFLOW_ACCOUNT_ID=AcCMptlq60zH
KISSFLOW_APP_ID=Expense_and_Travel_Management_A00
KISSFLOW_TRAVEL_PROCESS_ID=Travel_Management_A02
KISSFLOW_ACCESS_KEY_ID=
KISSFLOW_ACCESS_KEY_SECRET=

# Google Places (Train / Bus / Cab / Hotel location search)
# Leave empty to use Google-style mock results; set key later for live Maps Autocomplete
# GOOGLE_MAPS_API_KEY=

# Vite (web) — used only in local split-dev mode
VITE_API_PROXY=http://localhost:8080
